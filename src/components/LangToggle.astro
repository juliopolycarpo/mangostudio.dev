---
import type { Lang, SiteContent } from '@/i18n/types';
import { alternatePath } from '@/i18n/ui';

interface Props {
  lang: Lang;
  pathname: string;
  content: SiteContent;
}

const { lang, pathname, content } = Astro.props;
const options: { code: Lang; label: string }[] = [
  { code: 'pt', label: content.langToggle.pt },
  { code: 'en', label: content.langToggle.en },
];
---

<div class="lang-toggle" role="group" aria-label="Language">
  {
    options.map((option) => (
      <a
        href={alternatePath(pathname, option.code)}
        hreflang={option.code}
        class:list={['lang-opt', { 'is-active': lang === option.code }]}
        aria-current={lang === option.code ? 'true' : undefined}
        data-astro-prefetch={lang !== option.code ? 'hover' : undefined}
      >
        {option.label}
      </a>
    ))
  }
</div>
