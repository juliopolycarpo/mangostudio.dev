/// <reference types="astro/client" />

// Fontsource packages ship CSS only (no type declarations); these self-hosted
// font side-effect imports are resolved by Vite at build time.
declare module '@fontsource-variable/geist';
declare module '@fontsource-variable/geist-mono';
